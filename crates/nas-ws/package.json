{
  "name": "nas-ws",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "cargo watch -- cargo run --bin nas-ws",
    "seed": "cargo run --bin seed",
    "dockerize": "docker build ."
  },
  "dependencies": {}
}
